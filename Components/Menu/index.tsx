import * as React from 'react';

class Menu extends React.Component {
    constructor(props){
        super(props);
        console.log(props);
    }
    render(): React.ReactElement {
        return (<div></div>)
    }
}

export default Menu;
